spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/likelion-db
    username: root
    password: Environment_variables

# 환경 변수에 SPRING_DATASOURCE_PASSWORD=[비밀번호] 넣어줬음

